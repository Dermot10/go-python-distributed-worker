version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"

  go-service:
    build: .
    container_name: go-service
    depends_on:
      - redis
    ports:
      - "8081:8080"
    env_file:
      - .env
